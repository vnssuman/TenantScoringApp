# React & TypeScript Rules
- Use functional components with explicit TypeScript type props.
- Form inputs must validate types using Zod or standard TypeScript types before firing API calls to .NET.
- Never write `any` type in TypeScript.