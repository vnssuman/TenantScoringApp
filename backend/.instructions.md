# .NET 10 Coding Rules
- Apply `required` modifier on immutable DTO properties.
- Ensure all public service methods have interface contracts (e.g., `IScoringService`).
- Use C# pattern matching (`switch` expressions) for scoring calculations instead of nested `if-else` blocks.